import { Typography } from "@mui/material";

export default function CheckoutPage(){
    return(
        <Typography variant="h6">
            Only  a logged user can view this Order!!!
        </Typography>
    )
}